apiVersion: v1
kind: Service
metadata:
  name: {{ include "cascadia.fullname" . }}
  labels:
    {{- include "cascadia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cascadia.selectorLabels" . | nindent 4 }}
