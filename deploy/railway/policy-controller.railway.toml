# Railway config for the `policy-controller` service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/docker/policy-controller.Dockerfile"

[deploy]
startCommand = "cascadia-policy-controller"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
